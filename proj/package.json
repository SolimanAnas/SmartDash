{
  "name": "dcas-airport-ops",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DCAS Airport Operations Platform - modular source, single-file build",
  "scripts": {
    "build": "node build.mjs",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0",
    "globals": "^15.0.0",
    "prettier": "^3.2.0"
  }
}
